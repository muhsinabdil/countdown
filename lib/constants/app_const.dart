import 'package:flutter/material.dart';

const kPrimaryColor = Colors.blueGrey;

const double kBigRadus = 16.0;
const double kSmallRadus = 5.0;
const BorderRadius kDefaultBorderRadius =
    BorderRadius.all(Radius.circular(kSmallRadus));
const BorderRadius kBigBorderRadius =
    BorderRadius.all(Radius.circular(kBigRadus));
